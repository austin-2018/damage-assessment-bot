export default class ChatRegistrationService {
    
    public async register(): Promise<number> {
            
        // let chatAddressRepo = await ChatAddressRepo.getInstance();

        // let value: ChatLoginModel = req.body;

        // let address = await chatAddressRepo.get(value);

        return 9
    }
}